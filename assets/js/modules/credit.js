import {api} from "../services/api.js";
import {fmt,statusBadge} from "../core/format.js";
import {loading,empty,modal,serializeForm,toast} from "../core/ui.js";

export async function renderCredit(root){
  root.innerHTML=`<section class="page-head"><div><h2>Crédito comercial</h2><p>Radicación, análisis de cartera, aprobación y rechazo con trazabilidad.</p></div><div class="page-actions"><button class="btn btn-primary" id="new-credit">＋ Nueva solicitud</button></div></section><section class="card card-pad"><div class="toolbar"><input class="control search-wide" id="credit-search" placeholder="Solicitud, cliente o documento"><select class="control" id="credit-status"><option value="">Todos</option><option>SUBMITTED</option><option>UNDER_REVIEW</option><option>APPROVED</option><option>REJECTED</option></select><button class="btn btn-primary" id="credit-load">Buscar</button></div><div id="credit-result">${loading()}</div></section>`;
  async function load(){const target=root.querySelector("#credit-result");target.innerHTML=loading();const d=await api.creditList(root.querySelector("#credit-status").value||null,root.querySelector("#credit-search").value,1,100);target.innerHTML=d.items.length?table(d.items):empty();target.querySelectorAll("[data-credit-action]").forEach(b=>b.onclick=()=>transition(b.dataset.id,b.dataset.creditAction,load))}
  root.querySelector("#new-credit").onclick=()=>create(load);root.querySelector("#credit-load").onclick=load;root.querySelector("#credit-search").onkeydown=e=>{if(e.key==="Enter")load()};await load();
}
function table(rows){return `<div class="table-wrap"><table><thead><tr><th>Solicitud</th><th>Cliente</th><th>Valor</th><th>Plazo</th><th>Estado</th><th>Responsable</th><th>Fecha</th><th>Acciones</th></tr></thead><tbody>${rows.map(r=>`<tr><td><div class="cell-main">${fmt.escape(r.requestNumber)}</div></td><td>${fmt.escape(r.clientName)}<div class="cell-sub">${fmt.escape(r.clientDocument||"")}</div></td><td>${fmt.money(r.requestedAmount)}</td><td>${fmt.number(r.requestedTermDays)} días</td><td>${statusBadge(r.status)}<div class="cell-sub">${fmt.escape(r.decisionReason||"")}</div></td><td>${fmt.escape(r.assignedTo||r.requestedBy)}</td><td>${fmt.date(r.createdAt)}</td><td>${r.status==="SUBMITTED"?`<button class="btn btn-primary" data-id="${r.id}" data-credit-action="TAKE">Tomar</button>`:""}${r.status==="UNDER_REVIEW"?`<button class="btn btn-success" data-id="${r.id}" data-credit-action="APPROVE">Aprobar</button> <button class="btn btn-danger" data-id="${r.id}" data-credit-action="REJECT">Rechazar</button>`:""}</td></tr>`).join("")}</tbody></table></div>`}
function create(reload){modal({title:"Nueva solicitud de crédito",confirmLabel:"Radicar",body:`<form id="credit-form"><div class="form-grid"><div class="field"><label>Número</label><input class="control" name="requestNumber"></div><div class="field"><label>Cliente</label><input class="control" name="clientName" required></div><div class="field"><label>NIT / documento</label><input class="control" name="clientDocument"></div><div class="field"><label>Valor solicitado</label><input class="control" name="requestedAmount" type="number" step="any" required></div><div class="field"><label>Plazo en días</label><input class="control" name="requestedTermDays" type="number" required></div></div></form>`,onConfirm:async()=>{await api.creditCreate(serializeForm(document.querySelector("#credit-form")));toast("Solicitud de crédito radicada.");await reload()}})}
function transition(id,action,reload){if(action==="TAKE")return api.creditTransition(id,action).then(()=>{toast("Solicitud tomada por Cartera.");reload()});modal({title:action==="APPROVE"?"Aprobar crédito":"Rechazar crédito",confirmLabel:action==="APPROVE"?"Aprobar":"Rechazar",body:`<div class="field"><label>Motivo de la decisión</label><textarea class="control" id="credit-reason" required></textarea></div>`,onConfirm:async()=>{await api.creditTransition(id,action,document.querySelector("#credit-reason").value);toast("Decisión registrada.");await reload()}})}
