import {api} from "../services/api.js";
import {fmt,statusBadge} from "../core/format.js";
import {loading,toast,modal,empty} from "../core/ui.js";

export async function renderQa(root){
  root.innerHTML=`<section class="page-head"><div><h2>Pruebas automáticas del ERP</h2><p>Verifica rutas comerciales, controles operativos, concurrencia, inventario y continuidad del flujo.</p></div><div class="page-actions"><button class="btn btn-primary" id="run-all-qa">▶ Ejecutar validación completa</button><button class="btn btn-ghost" id="run-matrix">Probar 192 combinaciones</button><button class="btn btn-ghost" id="run-controls">Probar 10 controles</button></div></section>
  <section class="grid grid-3"><article class="card card-pad"><div class="kpi-label">Combinaciones comerciales</div><div class="kpi-value">192</div><div class="kpi-foot">Tipos de pedido, formas de pago, rutas, corte y compra</div></article><article class="card card-pad"><div class="kpi-label">Controles empresariales</div><div class="kpi-value">10</div><div class="kpi-foot">Concurrencia, puertas, entrega, historial e inventario</div></article><article class="card card-pad"><div class="kpi-label">Motor de operación</div><div class="kpi-value">1</div><div class="kpi-foot">Las pruebas utilizan el mismo flujo del ERP</div></article></section><div style="height:16px"></div><section class="card"><header class="card-head"><h3>Historial de pruebas</h3></header><div class="card-body" id="qa-runs">${loading()}</div></section>`;
  root.querySelector("#run-matrix").onclick=()=>runSuite(root,"matrix");root.querySelector("#run-controls").onclick=()=>runSuite(root,"controls");root.querySelector("#run-all-qa").onclick=()=>runSuite(root,"all");await load(root);
}
async function load(root){const runs=await api.qaRuns(50),target=root.querySelector("#qa-runs");target.innerHTML=runs.length?`<div class="table-wrap"><table style="min-width:900px"><thead><tr><th>Inicio</th><th>Tipo de prueba</th><th>Estado</th><th>Total</th><th>Aprobados</th><th>Fallidos</th><th>Duración</th><th>Detalle</th></tr></thead><tbody>${runs.map(r=>`<tr><td>${fmt.date(r.startedAt)}</td><td><span class="badge badge-blue"><span class="badge-dot"></span>${fmt.escape(fmt.suite(r.runType||"MATRIX"))}</span></td><td>${statusBadge(r.status)}</td><td>${r.totalScenarios}</td><td class="success">${r.passedScenarios}</td><td class="danger">${r.failedScenarios}</td><td>${r.completedAt?fmt.number((new Date(r.completedAt)-new Date(r.startedAt))/1000,1)+" s":"En ejecución"}</td><td><button class="btn btn-ghost" data-run="${r.id}">Abrir</button></td></tr>`).join("")}</tbody></table></div>`:empty("Sin pruebas ejecutadas","Ejecute la validación completa antes de habilitar la operación.");target.querySelectorAll("[data-run]").forEach(b=>b.onclick=()=>detail(b.dataset.run))}
async function runSuite(root,type){const buttons=[...root.querySelectorAll("#run-all-qa,#run-matrix,#run-controls")];buttons.forEach(b=>b.disabled=true);const main=root.querySelector("#run-all-qa"),original=main.textContent;try{if(type==="matrix"){main.textContent="Probando 192 combinaciones…";const r=await api.runQa(true);notify(r,"Combinaciones comerciales")}else if(type==="controls"){main.textContent="Probando 10 controles…";const r=await api.runQaControls(true);notify(r,"Controles empresariales")}else{main.textContent="Fase 1 de 2 · combinaciones comerciales…";const matrix=await api.runQa(true);notify(matrix,"Combinaciones comerciales");if(matrix.failed)throw new Error(`La primera fase terminó con ${matrix.failed} fallos. No se ejecutó la segunda fase.`);main.textContent="Fase 2 de 2 · controles empresariales…";const controls=await api.runQaControls(true);notify(controls,"Controles empresariales");if(!controls.failed)toast("Validación completa aprobada: 202 pruebas sin fallos.","success",9000)}await load(root)}catch(error){toast(error.message||String(error),"error",9000);throw error}finally{buttons.forEach(b=>b.disabled=false);main.textContent=original}}
function notify(result,label){toast(result.failed?`${label}: ${result.failed} fallos.`:`${label}: ${result.passed}/${result.total} aprobados.`,result.failed?"error":"success",7000)}
async function detail(id){const d=await api.qaDetail(id);modal({title:`Detalle de prueba ${id.slice(0,8)}`,confirmLabel:"",body:`<div class="qa-summary"><div class="qa-box"><span class="muted">Tipo</span><strong style="font-size:15px">${fmt.escape(fmt.suite(d.run.run_type||"MATRIX"))}</strong></div><div class="qa-box"><span class="muted">Total</span><strong>${d.run.total_scenarios}</strong></div><div class="qa-box"><span class="muted">Aprobados</span><strong class="success">${d.run.passed_scenarios}</strong></div><div class="qa-box"><span class="muted">Fallidos</span><strong class="danger">${d.run.failed_scenarios}</strong></div></div><div style="height:14px"></div><div class="table-wrap"><table><thead><tr><th>Escenario</th><th>Estado</th><th>Ruta esperada</th><th>Ruta obtenida</th><th>Error</th></tr></thead><tbody>${d.scenarios.map(s=>`<tr><td>${fmt.escape(fmt.label(s.scenario_key))}</td><td>${statusBadge(s.status)}</td><td><span>${fmt.escape(fmt.data(s.expected_path))}</span></td><td><span>${fmt.escape(fmt.data(s.actual_path))}</span></td><td class="danger">${fmt.escape(s.error_message||"")}</td></tr>`).join("")}</tbody></table></div>`})}
