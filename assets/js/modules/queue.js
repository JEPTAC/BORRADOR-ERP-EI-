import {api} from "../services/api.js";
import {fmt,statusBadge,priorityBadge} from "../core/format.js";
import {paginationHtml,empty,loading} from "../core/ui.js";
import {openOrder} from "./orders.js";

export async function renderQueue(root,{moduleId,steps,params={}}){
  let selected=params.step&&steps.includes(params.step)?params.step:steps[0],page=Number(params.page||1),assignment=params.assignment||"ALL";
  root.innerHTML=`<section class="page-head"><div><h2>Cola operativa</h2><p>Trabajo priorizado, SLA, responsables y estado de ejecución.</p></div><div class="page-actions"><button class="btn btn-ghost" id="mine">Mis tareas</button><button class="btn btn-ghost" id="all">Toda la cola</button></div></section>
  <section class="card card-pad"><div class="toolbar"><div id="step-tabs">${steps.map(s=>`<button class="btn ${s===selected?"btn-primary":"btn-ghost"}" data-step="${s}">${fmt.escape(s)}</button>`).join("")}</div><input class="control search-wide" id="queue-search" placeholder="Buscar pedido o cliente"><select class="control" id="queue-status"><option value="">Estados activos</option><option>QUEUED</option><option>ASSIGNED</option><option>IN_PROGRESS</option><option>WAITING</option><option>BLOCKED</option></select><button class="btn btn-primary" id="queue-filter">Filtrar</button></div><div id="queue-result">${loading()}</div></section>`;
  async function load(p=1){page=p;const result=root.querySelector("#queue-result");result.innerHTML=loading("Consultando cola…");const data=await api.listOrders({step:selected,status:root.querySelector("#queue-status").value||null,search:root.querySelector("#queue-search").value,assignment,page,pageSize:50,includeHistory:false});result.innerHTML=data.items.length?`${table(data.items)}${paginationHtml(data.pagination)}`:empty("Cola sin pedidos","No hay pedidos activos para esta etapa y filtros.");result.querySelectorAll("[data-order]").forEach(x=>x.onclick=()=>openOrder(x.dataset.order));result.querySelectorAll("[data-page]").forEach(x=>x.onclick=()=>load(Number(x.dataset.page)))}
  root.querySelectorAll("[data-step]").forEach(b=>b.onclick=()=>{selected=b.dataset.step;root.querySelectorAll("[data-step]").forEach(x=>{x.className=`btn ${x===b?"btn-primary":"btn-ghost"}`});load(1)});
  root.querySelector("#queue-filter").onclick=()=>load(1);root.querySelector("#queue-search").onkeydown=e=>{if(e.key==="Enter")load(1)};root.querySelector("#mine").onclick=()=>{assignment="MINE";load(1)};root.querySelector("#all").onclick=()=>{assignment="ALL";load(1)};await load(page);
}
function table(rows){return `<div class="table-wrap"><table><thead><tr><th>Prioridad</th><th>Pedido</th><th>Cliente</th><th>Estado</th><th>Responsable</th><th>Tiempo laboral</th><th>SLA</th><th>Ruta</th></tr></thead><tbody>${rows.map(o=>`<tr><td>${priorityBadge(o.priority)}</td><td><span class="table-link" data-order="${o.id}">${fmt.escape(o.orderNumber)}</span><div class="cell-sub">${fmt.escape(o.orderType)}</div></td><td><div class="cell-main">${fmt.escape(o.clientName)}</div><div class="cell-sub">${fmt.escape(o.paymentCondition)}</div></td><td>${statusBadge(o.status)}</td><td>${fmt.escape(o.assigneeName||"En cola")}<div class="cell-sub">${fmt.escape(o.roleCode||"")}</div></td><td>${fmt.hours(o.ageBusinessSeconds)}</td><td>${o.slaExceeded?'<span class="badge badge-red">Excedido</span>':'<span class="badge badge-green">En tiempo</span>'}</td><td>${fmt.escape(o.route)}</td></tr>`).join("")}</tbody></table></div>`}
