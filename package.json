{
  "name": "erp-electroingenieria-v10",
  "version": "10.25.10",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npx http-server . -p 4173 -c-1",
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:ui": "playwright test --ui",
    "validate": "node scripts/validate.mjs",
    "test:total": "playwright test tests/qa-total",
    "test:total:headed": "playwright test tests/qa-total --headed",
    "test:capacity": "k6 run tests/load/erp-capacity.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "http-server": "^14.1.1"
  }
}
